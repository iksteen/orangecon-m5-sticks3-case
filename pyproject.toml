[project]
name = "ocon"
version = "0.1.0"
description = "Build scripts for the M5StickS3 OrangeCon badge case."
requires-python = ">=3.10"
dependencies = [
    "fonttools>=4.0",
    "pillow>=10.0",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.11",
]

[tool.uv]
package = false
